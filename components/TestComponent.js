import React from 'react'

import './TestComponent.scss'

const TestComponent = () => {
  const hello = 'hello!';
  return (
    <div className="TestComponent">{hello}</div>
  );
}

export default TestComponent
